# To-Do's
- [ ] Store data using Chroma database
- [ ] Return the categories to the API
- [ ] Return the description to the API
- [ ] Return the summary to the API
- [ ] Create "day's summary" using LLMs
- [ ] Create websocket for real-time updates from feed
- [ ] Move `Broker.Close()` from use cases to `main.go`
- [ ] Handle the `context` in all subscribers